{
  "name": "test",
  "version": "1.0.1",
  "description": "tests for gulp-env-vars",
  "private": true,
  "author": "Jack.Chan",
  "scripts": {
    "build:dev": "yarn run gulp --conf=development",
    "build:test": "yarn run gulp --conf=test",
    "build:prod": "yarn run gulp --conf=production"
  },
  "browserslist": {
    "browsers": [
      ">1%",
      "last 4 versions",
      "Firefox ESR",
      "not ie < 9"
    ],
    "flexbox": "no-2009"
  },
  "dependencies": {},
  "devDependencies": {
    "through2": "^3.0.1",
    "argvs": "^1.0.1",
    "gulp": "^4.0.2"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
